# Data Structures

This section documents the core Python data structures practiced so far and their relevance to structured data.

## Lists

- Indexing and negative indexing
- Slicing
- Membership with `in`
- `append()`
- `insert()`
- `remove()`
- `pop()`
- `count()`
- `sort()`
- `sorted()`
- `reverse()`
- Copying lists

Important distinction practiced: `pop()` removes an element and returns the removed value, while `sort()` modifies the list in place and `sorted()` returns a sorted result.

## Tuples

- Basic tuple usage
- Indexing
- Immutability

## Sets

- Basic set usage
- Unique values
- Membership testing

## Dictionaries

- Key-value structure
- Accessing values by key
- Nested dictionaries
- Combining dictionaries with lists

## Nested Structured Data

A key pattern practiced was navigating structures such as:

`dictionary → list → dictionary → key`

This is particularly relevant when working with structured data returned by APIs or other data sources.

## Mutability and References

The difference between mutable and immutable objects, and the effect of modifying a referenced list, has been introduced and will continue to be reinforced through practice.

## Data Analytics Relevance

Lists, dictionaries and nested structures are fundamental when handling collections of records and structured data. Indexing, slicing, membership tests and list methods are frequently used during data preparation and transformation.

## Status

**In progress — continued practice required.**
